# Description: A VIM-inspired filemanager for the console 
# Homepage:    http://ranger.nongnu.org/
# Maintainer:  Eike Beyer eike dot beyer at yandex dot com
# Depends on:  python3 w3m file atool poppler

name=ranger
version=1.9.4
release=1
source=(https://github.com/ranger/ranger/archive/v$version.tar.gz)

build() {
    cd $name-$version

    python3 setup.py build
    python3 setup.py install --prefix=/usr --root=$PKG --optimize=1
}
